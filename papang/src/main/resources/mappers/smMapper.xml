<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.company.papang.impl.SmMapper">
	<!-- 단건조회 -->
	<select id="getCommunity" parameterType="communityVO" resultType="communityVO">
		SELECT  *
		FROM COMMUNITY
		WHERE COM_NO = #{com_no}
	</select>

	<!-- 전체조회 -->
	<select id="getCommunityList" resultType="communityVO">
		SELECT ROWNUM, C.* 
		FROM COMMUNITY C
		<where>
			<![CDATA[
				ROWNUM <= 1000
			]]>
			<if test="com_category != null">
				and com_category = #{com_category}
			</if>
		</where>
		ORDER BY C.COM_NO DESC
	</select>

	<!-- 커뮤니티 글등록 -->
	<insert id="communityFormInsert" parameterType="communityVO">
		<selectKey order="BEFORE" keyProperty="com_no" resultType="string">
			select SEQ_COMMUNITY_COM_NO.currval
			from dual
		</selectKey>
		INSERT INTO COMMUNITY (
		COM_NO,
		MBR_ID,
		COM_CATEGORY,
		COM_TITLE,
		COM_CONTENT
		)
		VALUES(
		#{com_no},
		'test',
		#{com_category},
		#{com_title},
		#{com_content}
		)
	</insert>


</mapper>
